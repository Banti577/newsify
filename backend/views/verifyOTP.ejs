<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Verify OTP</title>
  <style>
    * {
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: #f5f5f5;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .otp-container {
      background: #fff;
      padding: 30px 40px;
      border-radius: 12px;
      box-shadow: 0 0 15px rgba(0,0,0,0.1);
      max-width: 400px;
      width: 100%;
    }

    .otp-heading {
      text-align: center;
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 10px;
      color: #333;
    }

    .otp-subtext {
      font-size: 14px;
      color: #666;
      text-align: center;
      margin-bottom: 20px;
    }

    .otp-email {
      text-align: center;
      font-size: 15px;
      color: #444;
      margin-bottom: 20px;
    }

    .otp-input {
      width: 100%;
      padding: 12px 10px;
      margin-bottom: 20px;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 16px;
    }

    .otp-button {
      width: 100%;
      padding: 12px;
      background-color: #007bff;
      border: none;
      border-radius: 8px;
      color: white;
      font-size: 16px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .otp-button:hover {
      background-color: #0056b3;
    }

    .otp-error {
      color: red;
      text-align: center;
      font-size: 14px;
      margin-bottom: 10px;
    }

    .otp-success {
      color: green;
      text-align: center;
      font-size: 14px;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <div class="otp-container">
    <div class="otp-heading">OTP Verification</div>

    <div class="otp-subtext">Enter the 6-digit code sent to</div>
    <div class="otp-email"><%= email %></div>
<!--
    <% if (typeof error !== 'undefined') { %>
      <div class="otp-error"><%= error %></div>
    <% } %>

    <% if (typeof success !== 'undefined') { %>
      <div class="otp-success"><%= success %></div>
    <% } %> -->

    <form action="/user/verify-otp" method="POST">
      <input type="text" name="otp" maxlength="6" class="otp-input" placeholder="Enter OTP" required />
      <button type="submit" class="otp-button">Verify</button>
    </form>
  </div>
</body>
</html>
